import React from 'react';
import { cn } from '@/lib/utils';

/**
 * Centered-container layout for all pages of the Notification Package flow.
 *
 * Structure:
 *   gray page background → full-width wrapper → white card.
 *
 * Internal padding scales down on small screens.
 */
const NotificationFlowLayout = ({ children, className, cardClassName }) => (
  <div className={cn('bg-gray-50 dark:bg-slate-950 min-h-full pt-4 px-4 pb-4 sm:px-6 sm:pb-6 lg:px-8 lg:pb-8', className)}>
    <div className="w-full">
      <div
        className={cn(
          'bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-200/60 dark:border-slate-800',
          'p-4 sm:p-6 lg:p-8',
          cardClassName
        )}
      >
        {children}
      </div>
    </div>
  </div>
);

export default NotificationFlowLayout;
