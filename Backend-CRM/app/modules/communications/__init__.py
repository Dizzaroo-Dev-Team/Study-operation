"""communications module - conversations, threads, messages, attachments.

Moved from app/api/v1/endpoints/communications.py during the
v1/ ghost-folder elimination. The 1,555-line route file is still a
single module; splitting by domain (conversations / threads / messages /
attachments) is a separate follow-up.
"""
