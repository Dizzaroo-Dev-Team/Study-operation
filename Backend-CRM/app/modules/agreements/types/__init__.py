"""Agreement type sub-modules.

Each sibling sub-package (cda/, cta/, ...) is a self-contained implementation
of one agreement type. See ``app.modules.agreements.registry`` for the registry
contract and instructions for adding a new type.
"""
